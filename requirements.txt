# Runtime dependencies
requests
pandas
python-dotenv
networkx

# Web console (epic #6) — the Scryxen remediation console. `server.py` (FastAPI)
# serves web/ and exposes the engine over a JSON API; run with uvicorn.
fastapi
uvicorn

# Optional: ILP optimal-comparison stretch (epic #5, #35). Not needed for the
# core optimizer; optimizer_ilp.py and its tests skip when it is absent.
pulp

# Dev / test
pytest
